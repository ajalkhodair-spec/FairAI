services:
  ipfs:
    image: ipfs/kubo:v0.29.0
    container_name: fairai-ipfs
    ports:
      - "4001:4001"
      - "5001:5001"
      - "8080:8080"
    volumes:
      - fairai_ipfs_data:/data/ipfs
    environment:
      - IPFS_PROFILE=server

volumes:
  fairai_ipfs_data:
