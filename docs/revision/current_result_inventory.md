# Current Result Inventory

`results/q1_results_package/` contains:

- a 3-trial summary;
- local node metrics and approval outcomes;
- global model metrics;
- IPFS artifact CIDs and generic retrieval timings;
- proof-generation and verification fields;
- smart-contract events and selected gas values;
- an XLSX workbook, DOCX report, LaTeX snippets, and PNG/PDF figures.

The package reports the submitted baseline values, including two approved nodes,
one rejected node, global accuracy `0.938889`, global DP gap `0.144269`, and
global publication gas `449664`.

Classification: measured outputs are claimed by the package, but the package
does not contain a Git commit, complete environment manifest, configuration
hash, dataset checksum, partition checksum, or raw per-run directory. It cannot
yet satisfy revision-level provenance requirements.

