# Results

`q1_results_package/` contains the curated Q1 experiment outputs:

- spreadsheet tables
- raw CSV exports
- manuscript-ready figures
- LaTeX table and figure snippets
- a Word report

Generated runtime directories are ignored by default. Use `outputs/` locally for new experiment runs.
