# ZK Setup Files

Place `powersoftau_final.ptau` in this directory before running the full Groth16 setup.

The file is intentionally not committed because it is a generated proving-system setup artifact and can be large. To use a different local path, set:

```sh
FAIRAI_PTAU_PATH=/path/to/powersoftau_final.ptau
```
